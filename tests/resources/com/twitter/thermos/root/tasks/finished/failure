{"processes": [{"cmdline": "echo p1 pinging;                                echo ping >> p1;                                echo current count $(cat p1 | wc -l);           if [ $(cat p1 | wc -l) -eq 1 ]; then   exit 0;                                             else                                                    exit 1;                                             fi                                                    ", "daemon": 0, "max_failures": 5, "name": "p1"}, {"cmdline": "echo p2 pinging;                                echo ping >> p2;                                echo current count $(cat p2 | wc -l);           if [ $(cat p2 | wc -l) -eq 2 ]; then   exit 0;                                             else                                                    exit 1;                                             fi                                                    ", "daemon": 0, "max_failures": 5, "name": "p2"}, {"cmdline": "echo p3 pinging;                                echo ping >> p3;                                echo current count $(cat p3 | wc -l);           if [ $(cat p3 | wc -l) -eq 3 ]; then   exit 0;                                             else                                                    exit 1;                                             fi                                                    ", "daemon": 0, "max_failures": 5, "name": "p3"}], "name": "pingping", "max_failures": 1, "user": "{{thermos.user}}", "max_concurrency": 0, "resources": {"disk": 16384, "ram": 16777216, "cpu": 1.0}}